name: Ping Backend

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: # allows manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://valpicks-backend.onrender.com/health
